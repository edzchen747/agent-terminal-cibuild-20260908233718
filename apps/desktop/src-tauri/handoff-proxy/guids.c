// Defines the interface IIDs referenced by the MIDL-generated proxy
// (the header only declares them; including initguid.h first turns the
// declarations into definitions).
#include <initguid.h>
#include "ITerminalHandoff.h"